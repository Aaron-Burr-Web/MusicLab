<<<<<<< HEAD

---

## 2026.9.17 lisiqi 完成情况（按 Agent.md 问题清单）

| # | 问题 | 处理 |
| --- | --- | --- |
| 1 | 示例音轨按钮为空 | 用现有鼓 / 钢琴采样做成真正可播、可组合的四轨示例（`js/track-demo.js`） |
| 2 | 问卷结果没用上 | 结果按账号保存（`MusicLab.profile`）；首页欢迎面板按基础推荐起点：零基础→第一章，有基础→跳到「音程与和弦」（前面章节自动标记为跳过），基础扎实→游乐园 |
| 3 | 服务条款无页面 | 新增 `login/terms.html`，登录 / 注册页勾选框内的《用户服务条款》可点击 |
| 4 | 账号仅前端模拟 | 加了后端适配层 `MusicLab.api`：设置 `window.MUSICLAB_API_BASE` 后注册 / 登录 / 进度 / 反馈自动走接口，接口约定见 `docs/BACKEND.md`；未配置时保持本地模式 |
| 5 | 反馈不会真正发送 | 同上接口化；未接后端时提交后给出「通过邮件发送」的 mailto 兜底（地址在 `contact.html` 顶部常量，可改） |
| 6 | sitemap 域名 | 改为 GitHub Pages 地址 `https://aaron-burr-web.github.io/MusicLab/`（若实际域名不同请全局替换），删掉不存在的 sitemap.html 条目 |
| 7 | 播放定时不稳 | 新增 `js/audio-engine.js`：Web Audio 提前调度（后台也不掉拍）；双击本地文件打开（file://）时自动回退到 `<audio>` 池。游乐园已切换到该引擎 |
| 8 | 四轨不能一起播 | 游乐园四轨共用一个主时钟：顶部总控「全部播放 / 暂停 / 回到开头 / 总 BPM」，任一面板 ▶ 即同步加入；标尺定位对四轨同时生效。**和弦 / 贝斯 / 旋律轨现在会出声**（之前只有节奏轨有声音） |
| 9 | 创作不保存 | 游乐园内容（方块、调、调式、BPM）按账号自动保存到 localStorage，刷新恢复；登录 / 退出自动切换存档 |
| 10 | Playground 残留标签 | 已删除 |
| 11 | favicon / 编码 | 新增 `images/favicon.svg` 并在 20 个页面引入；加 `.editorconfig` 与 `.gitattributes` 强制 UTF-8（VS Code 装 EditorConfig 插件即可生效）；`Agent.md` 仍需 `git add` |
| 13 | 无自动化测试 | `npm test`（`tests/run.js`）：链接 / 锚点 / 搜索索引静态检查 + 20 个页面控制台报错 + 21 条交互用例（无需 npm install，只要本机有 Chrome / Edge） |

已知限制：本机测试环境里 `.wav` 的 HTTP 请求被安全软件拦截返回 204，所以 Web Audio 解码路径只在 file:// 回退模式下跑通了功能测试；引擎里对解码失败有逐文件回退，部署到真实服务器后请在浏览器里确认一次控制台没有 `[MusicLabAudio] 解码失败` 的提示。

---

## 2026.9.17（二）按成员意见调整

| 要求 | 处理 |
| --- | --- |
| 16 个示例按钮等游乐园 / 教学完工后再加 | 改为「敬请期待」禁用态（灰显、不可点），播放器脚本 `js/track-demo.js` 保留但在页面里注释掉，届时取消注释即可恢复 |
| 听音练习可以开始做，要用 JS 做进网站 | 新增 `js/ear-training.js`（纯 JS，用现有钢琴采样）：音程听辨、单音听辨（音程与和弦页）、和弦听辨（调式基础页）。每轮 8 题，可重听、给正误与解释、结束给正确率。等你发来案例后，按同样的挂载方式（`<div data-ear-training="...">`）替换题库即可 |
| QA 个性化跳转先不急 | 撤掉跳转与自动跳级，问卷结果仍按账号保存（`MusicLab.profile`），教学主页完工后直接启用即可 |
| 服务条款若做必须是弹窗 | 改为弹窗：正文统一在 `js/terms.js`，登录 / 注册页勾选框内的链接以弹窗打开，不跳走也不丢失已填内容；`login/terms.html` 整页版用同一份正文渲染 |
| 登录只做前端模拟，不要后端 API | 已移除之前加的 API 适配层，恢复纯前端；接口约定移到 `docs/BACKEND.md` 备查（标注为 10.11 之后的自愿项目） |
| 表单发送需要数据库，属后期 | 去掉接口调用，保留本地留档 + 邮件兜底 |
| sitemap 等全部页面完工后再应用 | 域名改回占位 `example.com`，文件顶部加 TODO 注释 |
| npm test 可以优先做 | 已就绪：`npm test`（静态链接检查 + 20 页控制台检查 + 32 条交互用例，零依赖） |
=======

>>>>>>> d55154fcad06af8c43f726c10e13f5a914be7fbc
